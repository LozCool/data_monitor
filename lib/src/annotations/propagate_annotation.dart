/// `PropagateAnnotation` provides the `@propagate` annotation for use in your data model classes.
class PropagateAnnotation
{
  const PropagateAnnotation();
}

const PropagateAnnotation propagate = PropagateAnnotation();
