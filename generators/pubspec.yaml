name: generators
description: 'Generators Package.'
homepage:
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  analyzer:
  build:
  source_gen:

  annotations:
    path: ../annotations/

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  flutter_lints:
