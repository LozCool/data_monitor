name: data_monitor
description: "A Data Monitor package that encapsulates Annotations, Example and Utilities."
repository: https://github.com/LozCool/data_monitor
version: 1.1.0

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

topics:
  - codegen
  - state-management
